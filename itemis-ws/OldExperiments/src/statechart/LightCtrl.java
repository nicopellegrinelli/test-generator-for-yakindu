/* Generated by itemis CREATE code generator. */
package statechart;

import com.yakindu.core.ICycleBased;
import com.yakindu.core.IStatemachine;
import com.yakindu.core.ITimed;
import com.yakindu.core.ITimerService;

public class LightCtrl implements ITimed, ICycleBased {
	private static class EvBuf {
		private boolean button;
	}
	private static class TimeEventsEvBuf {
		private boolean lightCtrl_main_region_Light_On_time_event_0;
	}
	private static class LightCtrlEvBuf {
		private EvBuf iface = new EvBuf();
		private TimeEventsEvBuf timeEvents = new TimeEventsEvBuf();
	}
	public enum State {
		MAIN_REGION_LIGHT_OFF,
		MAIN_REGION_LIGHT_ON,
		$NULLSTATE$
	};
	
	private final State[] stateVector = new State[1];
	
	private ITimerService timerService;
	
	private final boolean[] timeEvents = new boolean[1];
	
	private LightCtrlEvBuf current = new LightCtrlEvBuf();
	
	private boolean isExecuting;
	
	protected boolean getIsExecuting() {
		return isExecuting;
	}
	
	protected void setIsExecuting(boolean value) {
		this.isExecuting = value;
	}
	public LightCtrl() {
		for (int i = 0; i < 1; i++) {
			stateVector[i] = State.$NULLSTATE$;
		}
		
		clearInEvents();
		
		
		isExecuting = false;
	}
	
	public void runCycle() {
		/* Performs a 'run to completion' step. */
		if (timerService == null) {
			throw new IllegalStateException("Timer service must be set.");
		}
		if (this.operationCallback == null) {
			throw new IllegalStateException("Operation callback must be set.");
		}
		
		if (getIsExecuting()) {
			return;
		}
		isExecuting = true;
		swapInEvents();
		microStep();
		isExecuting = false;
	}
	
	public void enter() {
		/* Activates the state machine. */
		if (timerService == null) {
			throw new IllegalStateException("Timer service must be set.");
		}
		if (this.operationCallback == null) {
			throw new IllegalStateException("Operation callback must be set.");
		}
		
		if (getIsExecuting()) {
			return;
		}
		isExecuting = true;
		/* Default enter sequence for statechart LightCtrl */
		enterSequence_main_region_default();
		isExecuting = false;
	}
	
	public void exit() {
		/* Deactivates the state machine. */
		if (getIsExecuting()) {
			return;
		}
		isExecuting = true;
		/* Default exit sequence for statechart LightCtrl */
		exitSequence_main_region();
		stateVector[0] = State.$NULLSTATE$;
		isExecuting = false;
	}
	
	/**
	 * @see IStatemachine#isActive()
	 */
	public boolean isActive() {
		return stateVector[0] != State.$NULLSTATE$;
	}
	
	/** 
	* Always returns 'false' since this state machine can never become final.
	*
	* @see IStatemachine#isFinal()
	*/
	public boolean isFinal() {
		return false;
	}
	private void swapInEvents() {
		current.iface.button = button;
		button = false;
		current.timeEvents.lightCtrl_main_region_Light_On_time_event_0 = timeEvents[0];
		timeEvents[0] = false;
	}
	
	private void clearInEvents() {
		button = false;
		timeEvents[0] = false;
	}
	
	private void microStep() {
		switch (stateVector[0]) {
		case MAIN_REGION_LIGHT_OFF:
			main_region_Light_Off_react(-1l);
			break;
		case MAIN_REGION_LIGHT_ON:
			main_region_Light_On_react(-1l);
			break;
		default:
			break;
		}
	}
	
	/**
	* Returns true if the given state is currently active otherwise false.
	*/
	public boolean isStateActive(State state) {
	
		switch (state) {
		case MAIN_REGION_LIGHT_OFF:
			return stateVector[0] == State.MAIN_REGION_LIGHT_OFF;
		case MAIN_REGION_LIGHT_ON:
			return stateVector[0] == State.MAIN_REGION_LIGHT_ON;
		default:
			return false;
		}
	}
	
	public void setTimerService(ITimerService timerService) {
		this.timerService = timerService;
	}
	
	public ITimerService getTimerService() {
		return timerService;
	}
	
	public void raiseTimeEvent(int eventID) {
		timeEvents[eventID] = true;
	}
	
	
	public interface OperationCallback {
	
		public void setLight(boolean lightOn);
		
	}
	
	private OperationCallback operationCallback;
	
	public void setOperationCallback(OperationCallback operationCallback) {
		this.operationCallback = operationCallback;
	}
	
	private boolean button;
	
	
	public void raiseButton() {
		button = true;
	}
	
	/* Entry action for state 'Light On'. */
	private void entryAction_main_region_Light_On() {
		/* Entry action for state 'Light On'. */
		timerService.setTimer(this, 0, (5l * 1000l), false);
	}
	
	/* Exit action for state 'Light On'. */
	private void exitAction_main_region_Light_On() {
		/* Exit action for state 'Light On'. */
		timerService.unsetTimer(this, 0);
	}
	
	/* 'default' enter sequence for state Light Off */
	private void enterSequence_main_region_Light_Off_default() {
		/* 'default' enter sequence for state Light Off */
		stateVector[0] = State.MAIN_REGION_LIGHT_OFF;
	}
	
	/* 'default' enter sequence for state Light On */
	private void enterSequence_main_region_Light_On_default() {
		/* 'default' enter sequence for state Light On */
		entryAction_main_region_Light_On();
		stateVector[0] = State.MAIN_REGION_LIGHT_ON;
	}
	
	/* 'default' enter sequence for region main region */
	private void enterSequence_main_region_default() {
		/* 'default' enter sequence for region main region */
		react_main_region__entry_Default();
	}
	
	/* Default exit sequence for state Light Off */
	private void exitSequence_main_region_Light_Off() {
		/* Default exit sequence for state Light Off */
		stateVector[0] = State.$NULLSTATE$;
	}
	
	/* Default exit sequence for state Light On */
	private void exitSequence_main_region_Light_On() {
		/* Default exit sequence for state Light On */
		stateVector[0] = State.$NULLSTATE$;
		exitAction_main_region_Light_On();
	}
	
	/* Default exit sequence for region main region */
	private void exitSequence_main_region() {
		/* Default exit sequence for region main region */
		switch (stateVector[0]) {
		case MAIN_REGION_LIGHT_OFF:
			exitSequence_main_region_Light_Off();
			break;
		case MAIN_REGION_LIGHT_ON:
			exitSequence_main_region_Light_On();
			break;
		default:
			break;
		}
	}
	
	/* Default react sequence for initial entry  */
	private void react_main_region__entry_Default() {
		/* Default react sequence for initial entry  */
		enterSequence_main_region_Light_Off_default();
	}
	
	private long main_region_Light_Off_react(long transitioned_before) {
		/* The reactions of state Light Off. */
		long transitioned_after = transitioned_before;
		if (transitioned_after<0l) {
			if (current.iface.button) {
				exitSequence_main_region_Light_Off();
				operationCallback.setLight(true);
				enterSequence_main_region_Light_On_default();
				transitioned_after = 0l;
			}
		}
		/* If no transition was taken */
		if (transitioned_after==transitioned_before) {
			/* then execute local reactions. */
			transitioned_after = transitioned_before;
		}
		return transitioned_after;
	}
	
	private long main_region_Light_On_react(long transitioned_before) {
		/* The reactions of state Light On. */
		long transitioned_after = transitioned_before;
		if (transitioned_after<0l) {
			if (current.timeEvents.lightCtrl_main_region_Light_On_time_event_0) {
				exitSequence_main_region_Light_On();
				operationCallback.setLight(false);
				current.timeEvents.lightCtrl_main_region_Light_On_time_event_0 = false;
				enterSequence_main_region_Light_Off_default();
				transitioned_after = 0l;
			} else {
				if (current.iface.button) {
					exitSequence_main_region_Light_On();
					enterSequence_main_region_Light_On_default();
					transitioned_after = 0l;
				}
			}
		}
		/* If no transition was taken */
		if (transitioned_after==transitioned_before) {
			/* then execute local reactions. */
			transitioned_after = transitioned_before;
		}
		return transitioned_after;
	}
	
}
