/* Generated by itemis CREATE code generator. */
package statechart;

import com.yakindu.core.IEventDriven;
import com.yakindu.core.IStatemachine;
import com.yakindu.core.ITimed;
import com.yakindu.core.ITimerService;
import java.util.LinkedList;
import java.util.Queue;

public class C implements ITimed, IEventDriven {
	public enum State {
		MAIN_REGION_ZERO,
		MAIN_REGION_ONE,
		MAIN_REGION_INITIAL,
		$NULLSTATE$
	};
	
	private final State[] stateVector = new State[1];
	
	private ITimerService timerService;
	
	private final boolean[] timeEvents = new boolean[1];
	
	private Queue<Runnable> inEventQueue = new LinkedList<Runnable>();
	private boolean isExecuting;
	
	protected boolean getIsExecuting() {
		return isExecuting;
	}
	
	protected void setIsExecuting(boolean value) {
		this.isExecuting = value;
	}
	public C() {
		for (int i = 0; i < 1; i++) {
			stateVector[i] = State.$NULLSTATE$;
		}
		
		clearInEvents();
		
		/* Default init sequence for statechart C */
		setX(0l);
		
		isExecuting = false;
	}
	
	public void enter() {
		/* Activates the state machine. */
		if (timerService == null) {
			throw new IllegalStateException("Timer service must be set.");
		}
		
		if (getIsExecuting()) {
			return;
		}
		isExecuting = true;
		/* Default enter sequence for statechart C */
		enterSequence_main_region_default();
		isExecuting = false;
	}
	
	public void exit() {
		/* Deactivates the state machine. */
		if (getIsExecuting()) {
			return;
		}
		isExecuting = true;
		/* Default exit sequence for statechart C */
		exitSequence_main_region();
		stateVector[0] = State.$NULLSTATE$;
		isExecuting = false;
	}
	
	/**
	 * @see IStatemachine#isActive()
	 */
	public boolean isActive() {
		return stateVector[0] != State.$NULLSTATE$;
	}
	
	/** 
	* Always returns 'false' since this state machine can never become final.
	*
	* @see IStatemachine#isFinal()
	*/
	public boolean isFinal() {
		return false;
	}
	private void clearInEvents() {
		timeEvents[0] = false;
	}
	
	private void microStep() {
		switch (stateVector[0]) {
		case MAIN_REGION_ZERO:
			break;
		case MAIN_REGION_ONE:
			break;
		case MAIN_REGION_INITIAL:
			main_region_Initial_react(-1l);
			break;
		default:
			break;
		}
	}
	
	private void runCycle() {
		/* Performs a 'run to completion' step. */
		if (timerService == null) {
			throw new IllegalStateException("Timer service must be set.");
		}
		
		if (getIsExecuting()) {
			return;
		}
		isExecuting = true;
		nextEvent();
		do { 
			microStep();
			clearInEvents();
		} while (nextEvent());
		isExecuting = false;
	}
	
	protected boolean nextEvent() {
		if(!inEventQueue.isEmpty()) {
			inEventQueue.poll().run();
			return true;
		}
		return false;
	}
	/**
	* Returns true if the given state is currently active otherwise false.
	*/
	public boolean isStateActive(State state) {
	
		switch (state) {
		case MAIN_REGION_ZERO:
			return stateVector[0] == State.MAIN_REGION_ZERO;
		case MAIN_REGION_ONE:
			return stateVector[0] == State.MAIN_REGION_ONE;
		case MAIN_REGION_INITIAL:
			return stateVector[0] == State.MAIN_REGION_INITIAL;
		default:
			return false;
		}
	}
	
	public void setTimerService(ITimerService timerService) {
		this.timerService = timerService;
	}
	
	public ITimerService getTimerService() {
		return timerService;
	}
	
	public void raiseTimeEvent(int eventID) {
		inEventQueue.add(() -> {
			timeEvents[eventID] = true;
		});
		runCycle();
	}
	
	
	private long x;
	
	public long getX() {
		return x;
	}
	
	public void setX(long value) {
		this.x = value;
	}
	
	/* Entry action for state 'Initial'. */
	private void entryAction_main_region_Initial() {
		/* Entry action for state 'Initial'. */
		timerService.setTimer(this, 0, (1l * 1000l), false);
	}
	
	/* Exit action for state 'Initial'. */
	private void exitAction_main_region_Initial() {
		/* Exit action for state 'Initial'. */
		timerService.unsetTimer(this, 0);
	}
	
	/* 'default' enter sequence for state Zero */
	private void enterSequence_main_region_Zero_default() {
		/* 'default' enter sequence for state Zero */
		stateVector[0] = State.MAIN_REGION_ZERO;
	}
	
	/* 'default' enter sequence for state One */
	private void enterSequence_main_region_One_default() {
		/* 'default' enter sequence for state One */
		stateVector[0] = State.MAIN_REGION_ONE;
	}
	
	/* 'default' enter sequence for state Initial */
	private void enterSequence_main_region_Initial_default() {
		/* 'default' enter sequence for state Initial */
		entryAction_main_region_Initial();
		stateVector[0] = State.MAIN_REGION_INITIAL;
	}
	
	/* 'default' enter sequence for region main region */
	private void enterSequence_main_region_default() {
		/* 'default' enter sequence for region main region */
		react_main_region__entry_Default();
	}
	
	/* Default exit sequence for state Zero */
	private void exitSequence_main_region_Zero() {
		/* Default exit sequence for state Zero */
		stateVector[0] = State.$NULLSTATE$;
	}
	
	/* Default exit sequence for state One */
	private void exitSequence_main_region_One() {
		/* Default exit sequence for state One */
		stateVector[0] = State.$NULLSTATE$;
	}
	
	/* Default exit sequence for state Initial */
	private void exitSequence_main_region_Initial() {
		/* Default exit sequence for state Initial */
		stateVector[0] = State.$NULLSTATE$;
		exitAction_main_region_Initial();
	}
	
	/* Default exit sequence for region main region */
	private void exitSequence_main_region() {
		/* Default exit sequence for region main region */
		switch (stateVector[0]) {
		case MAIN_REGION_ZERO:
			exitSequence_main_region_Zero();
			break;
		case MAIN_REGION_ONE:
			exitSequence_main_region_One();
			break;
		case MAIN_REGION_INITIAL:
			exitSequence_main_region_Initial();
			break;
		default:
			break;
		}
	}
	
	/* The reactions of state null. */
	private void react_main_region__choice_0() {
		/* The reactions of state null. */
		if (getX()==1l) {
			enterSequence_main_region_One_default();
		} else {
			if (getX()==0l) {
				enterSequence_main_region_Zero_default();
			}
		}
	}
	
	/* Default react sequence for initial entry  */
	private void react_main_region__entry_Default() {
		/* Default react sequence for initial entry  */
		enterSequence_main_region_Initial_default();
	}
	
	private long main_region_Initial_react(long transitioned_before) {
		/* The reactions of state Initial. */
		long transitioned_after = transitioned_before;
		if (transitioned_after<0l) {
			if (timeEvents[0]) {
				exitSequence_main_region_Initial();
				setX(getX() + 1l);
				timeEvents[0] = false;
				react_main_region__choice_0();
				transitioned_after = 0l;
			}
		}
		/* If no transition was taken */
		if (transitioned_after==transitioned_before) {
			/* then execute local reactions. */
			transitioned_after = transitioned_before;
		}
		return transitioned_after;
	}
	
	/* Can be used by the client code to trigger a run to completion step without raising an event. */
	public void triggerWithoutEvent() {
		runCycle();
	}
}
