GeneratorModel for create::java {

	const PROJECT : string = "StatechartExamples"

	const FOLDER : string = "src"

	statechart SimpleChoice {
		
		feature Naming {
		    basePackage = "machines"
		}

		feature Outlet {
			targetProject = PROJECT
			targetFolder = FOLDER
			libraryTargetFolder = "src"
		}
	}

	statechart SimpleCompositeState {
		
		feature Naming {
		    basePackage = "machines"
		}
		
		feature Outlet {
			targetProject = PROJECT
			targetFolder = FOLDER
			libraryTargetFolder = "src"
		}
	}

	statechart SimpleLoop {
		
		feature Naming {
		    basePackage = "machines"
		}
		
		feature Outlet {
			targetProject = PROJECT
			targetFolder = FOLDER
			libraryTargetFolder = "src"
		}
	}

	statechart SimpleParallel {
		
		feature Naming {
		    basePackage = "machines"
		}
		
		feature Outlet {
			targetProject = PROJECT
			targetFolder = FOLDER
			libraryTargetFolder = "src"
		}
	}

	statechart SimpleSeries {

		feature Naming {
		    basePackage = "machines"
		}
		
		feature Outlet {
			targetProject = PROJECT
			targetFolder = FOLDER
			libraryTargetFolder = "src"
		}
	}
	
	statechart SimpleSeriesCycleBased {

		feature Naming {
		    basePackage = "machines"
		}
		
		feature Outlet {
			targetProject = PROJECT
			targetFolder = FOLDER
			libraryTargetFolder = "src"
		}
	}
}