testclass _easyChoiceTest for statechart _easyChoice {
	@Test
	operation test00 () {
	}

	@Test
	operation test01 () {
	    raise end
	    enter
	    raise prepare
	    raise prepare
	    raise prepare
	    raise start
	    raise end
	    assert is_active
	}

	@Test
	operation test02 () {
	    enter
	    raise prepare
	    raise prepare
	    raise prepare
	    raise start
	    raise end
	    assert is_active
	}

	@Test
	operation test03 () {
	    enter
	    raise prepare
	    raise prepare
	    raise start
	    raise start
	    assert is_active
	}

	@Test
	operation test04 () {
	    enter
	    raise prepare
	    raise start
	    raise prepare
	    assert is_active
	}

	@Test
	operation test05 () {
	    enter
	    raise start
	    assert is_active
	}

	@Test
	operation test06 () {
	    exit
	    assert !is_final
	}

	@Test
	operation test07 () {
	    enter
	    raise prepare
	    raise prepare
	    raise start
	    assert is_active
	    exit
	    assert !is_active
	}

	@Test
	operation test08 () {
	    enter
	    raise prepare
	    raise start
	    assert is_active
	    exit
	    assert !is_active
	}

	@Test
	operation test09 () {
	    enter
	    assert is_active
	    exit
	    assert !is_active
	}

	@Test
	operation test10 () {
	    enter
	    raise prepare
	    raise start
	    raise end
	    assert is_active
	    assert active (_easyChoice.main_region._final_)
	}

	@Test
	operation test11 () {
	    enter
	    raise prepare
	    raise prepare
	    raise prepare
	    raise start
	    assert is_active
	    assert active (_easyChoice.main_region.A_lot_of_times)
	}

	@Test
	operation test12 () {
	    enter
	    raise prepare
	    raise prepare
	    raise start
	    assert is_active
	    assert active (_easyChoice.main_region.TwoTimes)
	}

	@Test
	operation test13 () {
	    enter
	    raise prepare
	    raise start
	    assert is_active
	    assert active (_easyChoice.main_region.OneTime)
	}

	@Test
	operation test14 () {
	    enter
	    assert is_active
	    assert active (_easyChoice.main_region.Start)
	}

	@Test
	operation test15 () {
	}

	@Test
	operation test16 () {
	    assert !active (_easyChoice.main_region.A_lot_of_times)
	}

	@Test
	operation test17 () {
	    assert !active (_easyChoice.main_region.TwoTimes)
	}

	@Test
	operation test18 () {
	    assert !active (_easyChoice.main_region.OneTime)
	}

	@Test
	operation test19 () {
	    assert !active (_easyChoice.main_region._final_)
	}

	@Test
	operation test20 () {
	    assert !active (_easyChoice.main_region.Start)
	}

	@Test
	operation test21 () {
	    triggerWithoutEvent
	    assert !is_final
	}

	@Test
	operation test22 () {
	    assert !is_active
	    enter
	    raise prepare
	    raise prepare
	    raise prepare
	    raise start
	    raise start
	    assert is_active
	}

	@Test
	operation test23 () {
	    enter
	    raise prepare
	    raise start
	    raise end
	    assert is_final
	}

	@Test
	operation test24 () {
	    assert !is_active
	    assert !is_final
	}

	@Test
	operation test25 () {
	    assert !is_active
	    enter
	    assert is_active
	}

	@Test
	operation test26 () {
	    assert !is_active
	}

	@Test
	operation test27 () {
	    enter
	    raise prepare
	    raise prepare
	    raise start
	    raise end
	    assert is_final
	}

	@Test
	operation test28 () {
	    enter
	    raise prepare
	    raise start
	    raise end
	    assert is_active
	    exit
	    assert !is_active
	}

	@Test
	operation test29 () {
	    enter
	    raise prepare
	    raise start
	    raise end
	    triggerWithoutEvent
	    assert is_final
	}

	@Test
	operation test30 () {
	    enter
	    raise prepare
	    raise prepare
	    raise prepare
	    raise start
	    assert is_active
	    exit
	    assert !is_active
	}

}