GeneratorModel for create::java {

	const PROJECT : string = "Temp"

	const FOLDER : string = "src"

	statechart ExampleStc {

		feature Naming {
		    basePackage = "temp"
		}

		feature Outlet {
			targetProject = PROJECT
			targetFolder = FOLDER
			libraryTargetFolder = "src"
		}
	}
}