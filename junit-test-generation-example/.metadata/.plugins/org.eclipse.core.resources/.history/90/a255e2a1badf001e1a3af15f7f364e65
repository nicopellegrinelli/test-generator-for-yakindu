testclass easyChoiceTest for statechart easyChoice {
	@Test
	operation test00 () {
	}

	@Test
	operation test01 () {
	    enter
	    raise prepare
	    raise prepare
	    raise start
	    raise start
	    assert is_active
	}

	@Test
	operation test02 () {
	    enter
	    raise prepare
	    raise start
	    triggerWithoutEvent
	    assert is_active
	}

	@Test
	operation test03 () {
	    enter
	    raise start
	    assert is_active
	}

	@Test
	operation test04 () {
	    exit
	    assert !is_final
	}

	@Test
	operation test05 () {
	    enter
	    raise prepare
	    raise prepare
	    raise prepare
	    raise start
	    assert is_active
	    exit
	    assert !is_active
	}

	@Test
	operation test06 () {
	    enter
	    raise prepare
	    raise start
	    assert is_active
	    exit
	    assert !is_active
	}

	@Test
	operation test07 () {
	    enter
	    assert is_active
	    exit
	    assert !is_active
	}

	@Test
	operation test08 () {
	    enter
	    assert is_active
	    assert active (easyChoice.main_region.Start)
	}

	@Test
	operation test09 () {
	    assert !active (easyChoice.main_region._final_)
	}

	@Test
	operation test10 () {
	    assert !active (easyChoice.main_region.A_lot_of_times)
	}

	@Test
	operation test11 () {
	    assert !active (easyChoice.main_region.TwoTimes)
	}

	@Test
	operation test12 () {
	    assert !active (easyChoice.main_region.OneTime)
	}

	@Test
	operation test13 () {
	}

	@Test
	operation test14 () {
	    assert !active (easyChoice.main_region.Start)
	}

	@Test
	operation test15 () {
	    enter
	    raise prepare
	    raise start
	    raise end
	    assert is_active
	    assert is_final
	}

	@Test
	operation test16 () {
	    assert !is_final
	}

	@Test
	operation test17 () {
	    enter
	    assert is_active
	}

	@Test
	operation test18 () {
	    assert !is_active
	}

	@Test
	operation test19 () {
	    enter
	    raise prepare
	    raise start
	    raise end
	    raise start
	    assert is_active
	}

	@Test
	operation test20 () {
	    enter
	    raise prepare
	    raise prepare
	    raise start
	    raise end
	    assert is_active
	}

	@Test
	operation test21 () {
	    assert !is_active
	    triggerWithoutEvent
	    enter
	    raise prepare
	    raise start
	    triggerWithoutEvent
	    assert is_active
	}

	@Test
	operation test22 () {
	    enter
	    raise prepare
	    raise prepare
	    raise prepare
	    raise start
	    raise end
	    assert is_final
	}

	@Test
	operation test23 () {
	    enter
	    raise prepare
	    raise start
	    raise end
	    assert is_active
	    exit
	    assert !is_active
	}

}