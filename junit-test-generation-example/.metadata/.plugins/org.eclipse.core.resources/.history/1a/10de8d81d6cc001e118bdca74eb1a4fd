testclass ExampleStcTest for statechart ExampleStc {
	@Test
	operation test09 () {
	    enter
	    raise power_on
	    raise simplified
	    raise a
	    assert is_active
	}

	@Test
	operation test10 () {
	    enter
	    raise power_on
	    raise simplified
	    raise start
	    raise a
	    assert !is_final
	}

	@Test
	operation test11 () {
	    enter
	    raise power_on
	    raise simplified
	    raise power_on
	    assert is_active
	}

	@Test
	operation test12 () {
	    enter
	    raise power_on
	    raise end
	    assert is_active
	}

	@Test
	operation test13 () {
	    enter
	    raise start
	    assert is_active
	}

	@Test
	operation test14 () {
	    enter
	    raise power_on
	    raise simplified
	    raise start
	    raise end_all
	    assert is_active
	}

	@Test
	operation test15 () {
	    enter
	    raise power_on
	    raise simplified
	    raise end_all
	    assert is_active
	}

	@Test
	operation test17 () {
	    enter
	    raise power_on
	    raise simplified
	    exit
	    assert !is_active
	}

	@Test
	operation test18 () {
	    enter
	    raise power_on
	    raise work
	    assert is_active
	    exit
	    assert !is_active
	}

	@Test
	operation test19 () {
	    enter
	    raise power_on
	    assert is_active
	    exit
	    assert !is_active
	}

	@Test
	operation test20 () {
	    enter
	    assert is_active
	    exit
	    assert !is_active
	}

	@Test
	operation test21 () {
	    enter
	    raise power_on
	    raise work
	    raise end_all
	    assert is_final
	    assert active (ExampleStc.main_region._final_)
	}

	@Test
	operation test22 () {
	    enter
	    raise power_on
	    raise simplified
	    raise start
	    assert is_active
	    assert active (ExampleStc.main_region.On.r_0.Simplified_analyses012.r_2.an2_a)
	}

	@Test
	operation test23 () {
	    enter
	    assert is_active
	    assert !active (ExampleStc.main_region.On.r_0.Simplified_analyses012)
	}

	@Test
	operation test24 () {
	    enter
	    raise power_on
	    raise simplified
	    assert is_active
	    assert active (ExampleStc.main_region.On.r_0.Init_anAlyses)
	}

	@Test
	operation test25 () {
	    enter
	    raise power_on
	    raise simplified
	    assert is_active
	    assert active (ExampleStc.main_region.On)
	}

	@Test
	operation test26 () {
	    enter
	    assert is_active
	    assert !active (ExampleStc.main_region.On)
	}

	@Test
	operation test27 () {
	    enter
	    raise power_on
	    assert is_active
	    assert active (ExampleStc.main_region.Simplified)
	}

	@Test
	operation test28 () {
	    enter
	    assert is_active
	    assert active (ExampleStc.main_region.Off_)
	}

	@Test
	operation test45 () {
	    enter
	    raise power_on
	    raise work
	    raise end_all
	    assert is_active
	    assert is_final
	}

	@Test
	operation test47 () {
	    enter
	    raise power_on
	    raise work
	    raise end
	    assert is_active
	    assert is_final
	}

	@Test
	operation test49 () {
	    enter
	    assert is_active
	}

	@Test
	operation test51 () {
	    enter
	    raise power_on
	    raise simplified
	    raise start
	    assert is_active
	    exit
	    assert !is_active
	}

	@Test
	operation test53 () {
	    enter
	    raise power_on
	    raise work
	    raise end_all
	    assert is_active
	    exit
	    assert !is_active
	}

	@Test
	operation test55 () {
	    enter
	    raise power_on
	    raise work
	    raise end_all
	    raise end
	    assert is_final
	}

	@Test
	operation test56 () {
	    enter
	    raise power_on
	    raise work
	    raise end
	    raise end_all
	    assert is_final
	}

	@Test
	operation test57 () {
	    enter
	    raise power_on
	    raise simplified
	    raise start
	    raise b
	    assert is_active
	    exit
	    assert !is_active
	}

}