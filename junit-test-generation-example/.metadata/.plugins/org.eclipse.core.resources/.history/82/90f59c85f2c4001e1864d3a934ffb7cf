testclass StatechartTest for statechart Statechart {
	@Test
	operation test06 () {
	    enter
	    raise power_on
	    proceed 1 cycle
	    proceed 1 cycle
	    assert is_active
	}

	@Test
	operation test07 () {
	    enter
	    proceed 1 cycle
	    assert is_active
	}

	@Test
	operation test09 () {
	    raise power_on
	    enter
	    proceed 1 cycle
	    assert is_active
	    exit
	    assert !is_active
	}

	@Test
	operation test10 () {
	    enter
	    assert is_active
	    exit
	    assert !is_active
	}

	@Test
	operation test11 () {
	    enter
	    assert is_active
	    assert !active (Statechart.main_region.on.r1.analyses)
	}

	@Test
	operation test12 () {
	    raise power_on
	    enter
	    proceed 1 cycle
	    raise work
	    proceed 1 cycle
	    assert is_active
	    assert active (Statechart.main_region.main_region)
	}

	@Test
	operation test13 () {
	    enter
	    assert is_active
	    assert !active (Statechart.main_region.on)
	}

	@Test
	operation test14 () {
	    raise power_on
	    enter
	    proceed 1 cycle
	    assert is_active
	    assert active (Statechart.main_region.idle)
	}

	@Test
	operation test15 () {
	    enter
	    assert is_active
	    assert active (Statechart.main_region.main_region)
	}

	@Test
	operation test30 () {
	    raise power_on
	    assert !is_active
	    enter
	    proceed 1 cycle
	    raise analize
	    proceed 1 cycle
	    proceed 1 cycle
	    assert is_active
	}

	@Test
	operation test38 () {
	    raise power_on
	    enter
	    proceed 1 cycle
	    raise analize
	    proceed 1 cycle
	    assert is_active
	    exit
	    assert !is_active
	}

	@Test
	operation test40 () {
	    raise power_on
	    enter
	    proceed 1 cycle
	    raise work
	    proceed 1 cycle
	    assert is_active
	    exit
	    assert !is_active
	}

	@Test
	operation test42 () {
	    assert !is_active
	    enter
	    assert is_active
	}

}