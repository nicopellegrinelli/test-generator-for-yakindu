GeneratorModel for create::java {

	const PROJECT : string = "TestResources"

	const FOLDER : string = "src"

	statechart javareading_statechart {

		feature Outlet {
			targetProject = PROJECT
			targetFolder = FOLDER
			libraryTargetFolder = "src"
		}
	}

	statechart javareading_submachine {

		feature Outlet {
			targetProject = PROJECT
			targetFolder = FOLDER
			libraryTargetFolder = "src"
		}
	}
}