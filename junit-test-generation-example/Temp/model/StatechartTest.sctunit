testclass StatechartSimplifiedTest for statechart Statechart {

	@Test
	operation test00 () {
	    enter
	    raise power_on
	    raise analize
	    raise start
	    raise b
	    raise a
	    assert is_active
	    assert active (StatechartSimplified.mainregion.on)
	}

	@Test
	operation test02 () {
	    enter
	    raise b
	    assert is_active
	}

	@Test
	operation test03 () {
	    enter
	    raise power_on
	    raise analize
	    raise start
	    assert is_active
	    assert active (StatechartSimplified.mainregion.on.r1.analyses.r1.an1)
	}

	@Test
	operation test06 () {
	    enter
	    raise power_on
	    raise work
	    triggerWithoutEvent
	    assert is_active
	}

	@Test
	operation test07 () {
	    enter
	    raise power_on
	    raise analize
	    raise start
	    raise b
	    raise a
	    raise start
	    assert is_active
	}

	@Test
	operation test08 () {
	    enter
	    raise power_on
	    raise analize
	    raise start
	    raise a
	    assert is_active
	}

	@Test
	operation test09 () {
	    enter
	    raise power_on
	    raise b
	    assert is_active
	}

	@Test
	operation test10 () {
	    enter
	    raise power_on
	    raise analize
	    raise start
	    raise b
	    raise end_all
	    assert is_active
	}

	@Test
	operation test11 () {
	    enter
	    raise power_on
	    raise analize
	    raise start
	    enter
	    raise end_all
	    assert is_active
	}

	@Test
	operation test12 () {
	    enter
	    raise power_on
	    raise analize
	    raise start
	    raise b
	    assert is_active
	    exit
	    assert !is_active
	}

	@Test
	operation test14 () {
	    enter
	    raise power_on
	    raise analize
	    raise start
	    raise b
	    raise a
	    assert is_active
	    exit
	    assert !is_active
	}

	@Test
	operation test15 () {
	    enter
	    raise power_on
	    raise analize
	    raise start
	    exit
	    assert !is_active
	}

	@Test
	operation test16 () {
	    enter
	    raise power_on
	    raise analize
	    assert is_active
	    exit
	    assert !is_active
	}

	@Test
	operation test17 () {
	    enter
	    raise power_on
	    raise work
	    assert is_active
	    exit
	    assert !is_active
	}

	@Test
	operation test18 () {
	    enter
	    raise power_on
	    assert is_active
	    exit
	    assert !is_active
	}

	@Test
	operation test19 () {
	    enter
	    assert is_active
	    exit
	    assert !is_active
	}

	@Test
	operation test20 () {
	    enter
	    raise power_on
	    raise work
	    raise end_all
	    assert is_active
	    assert active (StatechartSimplified.mainregion._final_)
	}

	@Test
	operation test21 () {
	    enter
	    raise power_on
	    raise analize
	    raise start
	    raise b
	    raise a
	    assert is_active
	    assert active (StatechartSimplified.mainregion.on.r1.endanalyses)
	}

	@Test
	operation test23 () {
	    enter
	    raise power_on
	    raise analize
	    raise start
	    assert active (StatechartSimplified.mainregion.on.r1.analyses.r2.an2a)
	}

	@Test
	operation test24 () {
	    enter
	    raise power_on
	    raise analize
	    raise start
	    raise b
	    enter
	    raise end
	    assert is_active
	}

	@Test
	operation test25 () {
	    enter
	    raise power_on
	    raise analize
	    raise start
	    assert is_active
	    assert active (StatechartSimplified.mainregion.on.r1.analyses)
	}

	@Test
	operation test26 () {
	    enter
	    assert is_active
	    assert !active (StatechartSimplified.mainregion.on.r1.analyses)
	}

	@Test
	operation test27 () {
	    enter
	    raise power_on
	    raise analize
	    assert is_active
	    assert active (StatechartSimplified.mainregion.on.r1.initanalyses)
	}

	@Test
	operation test28 () {
	    enter
	    raise power_on
	    raise analize
	    assert active (StatechartSimplified.mainregion.on)
	}

	@Test
	operation test29 () {
	    enter
	    assert is_active
	    assert !active (StatechartSimplified.mainregion.on)
	}

	@Test
	operation test30 () {
	    enter
	    raise power_on
	    assert is_active
	    assert active (StatechartSimplified.mainregion.idle)
	}

	@Test
	operation test31 () {
	    enter
	    assert is_active
	    assert active (StatechartSimplified.mainregion.off)
	}

	@Test
	operation test45 () {
	    assert !is_active
	    enter
	    assert is_active
	}

	@Test
	operation test46 () {
	    enter
	    raise power_on
	    raise analize
	    raise end_all
	    raise end_all
	    assert is_final
	}

	@Test
	operation test47 () {
	    assert !is_active
	    enter
	    raise power_on
	    raise analize
	    raise start
	    raise b
	    raise a
	    raise end
	    raise power_on
	    assert is_active
	}

	@Test
	operation test48 () {
	    enter
	    raise power_on
	    raise work
	    raise end_all
	    assert is_final
	}

	@Test
	operation test49 () {
	    enter
	    raise power_on
	    raise analize
	    raise end_all
	    assert is_final
	    exit
	    assert !is_active
	}

}
