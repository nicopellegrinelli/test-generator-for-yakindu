testclass StatechartTest for statechart Statechart {
	@Test
	operation test01 () {
	    enter
	    raise b
	    assert is_active
	}

	@Test
	operation test04 () {
	    enter
	    raise power_on
	    raise analyze
	    raise start
	    raise b
	    raise a
	    raise end
	    raise b
	    assert !is_final
	}

	@Test
	operation test05 () {
	    enter
	    raise power_on
	    raise analyze
	    raise b
	    assert is_active
	}

	@Test
	operation test06 () {
	    enter
	    raise power_on
	    raise work
	    raise end
	    raise a
	    assert is_active
	}

	@Test
	operation test07 () {
	    enter
	    raise power_on
	    raise analyze
	    raise start
	    raise b
	    raise a
	    raise a
	    assert is_active
	}

	@Test
	operation test08 () {
	    enter
	    raise power_on
	    raise analyze
	    raise start
	    raise a
	    assert is_active
	}

	@Test
	operation test09 () {
	    enter
	    raise power_on
	    raise a
	    assert is_active
	}

	@Test
	operation test10 () {
	    enter
	    raise power_on
	    raise analyze
	    raise start
	    raise b
	    raise end_all
	    assert is_active
	}

	@Test
	operation test11 () {
	    enter
	    raise power_on
	    raise analyze
	    raise start
	    enter
	    raise end_all
	    assert is_active
	}

	@Test
	operation test12 () {
	    enter
	    raise power_on
	    raise work
	    raise end_all
	    assert is_active
	}

	@Test
	operation test13 () {
	    enter
	    raise power_on
	    raise analyze
	    raise start
	    raise b
	    assert is_active
	    exit
	    assert !is_active
	}

	@Test
	operation test15 () {
	    enter
	    raise power_on
	    raise work
	    raise end
	    assert is_active
	    exit
	    assert !is_active
	}

	@Test
	operation test16 () {
	    enter
	    raise power_on
	    raise analyze
	    raise start
	    assert is_active
	    exit
	    assert !is_active
	}

	@Test
	operation test17 () {
	    enter
	    raise power_on
	    raise analyze
	    assert is_active
	    exit
	    assert !is_active
	}

	@Test
	operation test18 () {
	    enter
	    raise power_on
	    raise work
	    assert is_active
	    exit
	    assert !is_active
	}

	@Test
	operation test19 () {
	    enter
	    raise power_on
	    assert is_active
	    exit
	    assert !is_active
	}

	@Test
	operation test20 () {
	    enter
	    assert is_active
	    exit
	    assert !is_active
	}

	@Test
	operation test21 () {
	    enter
	    raise power_on
	    raise analyze
	    raise end_all
	    assert is_active
	    assert active (Statechart.main_region._final_)
	}

	@Test
	operation test23 () {
	    enter
	    assert is_active
	    assert !active (Statechart.main_region.On.r_0.analyses012)
	}

	@Test
	operation test24 () {
	    enter
	    raise power_on
	    raise analyze
	    assert is_active
	    assert active (Statechart.main_region.On.r_0.Init_anAlyses)
	}

	@Test
	operation test25 () {
	    enter
	    raise power_on
	    raise work
	    assert is_active
	    assert active (Statechart.main_region.On.r_0.working)
	}

	@Test
	operation test26 () {
	    enter
	    raise power_on
	    raise work
	    assert is_active
	    assert active (Statechart.main_region.On)
	}

	@Test
	operation test27 () {
	    enter
	    assert is_active
	    assert !active (Statechart.main_region.On)
	}

	@Test
	operation test28 () {
	    enter
	    raise power_on
	    assert is_active
	    assert active (Statechart.main_region.idle)
	}

	@Test
	operation test29 () {
	    enter
	    assert is_active
	    assert active (Statechart.main_region.Off_)
	}

	@Test
	operation test42 () {
	    enter
	    raise power_on
	    raise analyze
	    raise start
	    enter
	    raise b
	    raise a
	    assert is_active
	}

	@Test
	operation test43 () {
	    enter
	    raise power_on
	    raise analyze
	    raise end_all
	    assert is_active
	    assert is_final
	}

	@Test
	operation test45 () {
	    enter
	    raise power_on
	    raise work
	    raise end
	    assert is_active
	    assert is_final
	}

	@Test
	operation test47 () {
	    enter
	    assert is_active
	}

	@Test
	operation test48 () {
	    enter
	    raise power_on
	    raise analyze
	    raise end_all
	    assert is_active
	    exit
	    assert !is_active
	}

	@Test
	operation test49 () {
	    enter
	    raise power_on
	    raise analyze
	    raise end_all
	    raise b
	    assert is_active
	}

	@Test
	operation test50 () {
	    enter
	    raise power_on
	    raise analyze
	    raise start
	    raise b
	    raise a
	    raise end
	    raise end
	    assert is_final
	}

	@Test
	operation test52 () {
	    enter
	    raise power_on
	    raise work
	    raise end
	    raise end_all
	    assert is_final
	}

}